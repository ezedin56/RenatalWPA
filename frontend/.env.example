# API base URL — must include the /api suffix (matches axios baseURL in src/utils/api.ts).
# For production builds, set this to your deployed backend (e.g. https://api.example.com/api).
VITE_API_URL=http://localhost:5000/api
